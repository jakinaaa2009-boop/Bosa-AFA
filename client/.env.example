# Production (Vercel): public URL of your Railway API — include https:// and no trailing slash.
# Example: https://promo-api-production.up.railway.app
# (Host-only like promo-api.up.railway.app also works; https:// is added automatically.)
# Local dev: leave empty so Vite proxies /api and /uploads to the server.
VITE_API_BASE_URL=
